{
  "mcpServers": {
    "aipi": {
      "command": "node",
      "args": ["${PLUGIN_ROOT}/scripts/api-forge-server.mjs"],
      "cwd": "${PLUGIN_ROOT}",
      "env": {
        "AIPI_DATA": "${PLUGIN_DATA}"
      }
    }
  }
}
