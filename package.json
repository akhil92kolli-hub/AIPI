{
  "name": "ai-pi-showcase",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && npx wrangler deploy --config ./wrangler.jsonc --no-autoconfig"
  },
  "devDependencies": {
    "wrangler": "^4.136.2"
  }
}
